apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app: {{ .Release.Name }}
data:
  api-url: {{ .Values.env.NEXT_PUBLIC_TASK_API_URL}}
  backend-url: {{ .Values.env.NEXT_PUBLIC_TASK_BACKEND_URL}}

